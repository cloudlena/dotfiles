[user]
	name = Lena Fuhrimann
	email = cloudlena@users.noreply.github.com
[github]
    user = mastertinner
[core]
    pager = diff-so-fancy | less --tabs=4 -RFX
[pull]
    ff = only
[commit]
	gpgsign = true
[diff]
	tool = nvimdiff
[difftool]
	prompt = false
[merge]
	tool = nvimdiff
[mergetool]
	prompt = false
[pull]
	ff = only

; diff-so-fancy config
[core]
	pager = diff-so-fancy | less --tabs=4 -RFX
[interactive]
	diffFilter = diff-so-fancy --patch
